pipeline {
    agent any

    stages {
        stage('Generate') {
            steps {
                checkout scm
            } 
        }    
        stage('Build') {
            steps {
                echo 'Build project'
                sh "chmod +x gradlew"
                sh "./gradlew build --stacktrace" //run a gradle task
                    }
            }      
        stage('Unit and Integration Test') {
            steps {
                echo 'Testing..'
                junit '**/backend/src/test/gameoflife*.xml'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}