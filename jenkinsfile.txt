pipeline {
    agent any

    stages {
        stage('Generate') {
            steps {
                checkout scm
            } 
        }    
        stage('Build') {
            steps {
                echo 'Build project'
                sh "chmod +x gradlew"
                sh "./gradlew build --stacktrace" //run a gradle task
                    }
            }      
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}